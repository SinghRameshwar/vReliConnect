import { StyleSheet } from "react-native";

const DuplicateEventStyle = StyleSheet.create({
    duplicateBtnContainer: {
        backgroundColor: "#84FFB5",
        alignItems: "center",
        marginTop: 16,
        borderRadius: 6,
    }
});

export default DuplicateEventStyle;
